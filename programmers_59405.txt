/*입양 시각 구하기(2)
동물 보호소에 가장 먼저 들어온 동물의 이름을 조회하는 SQL 문을 작성해주세요.
*/
-- 코드를 입력하세요

#
SELECT NAME
FROM ANIMAL_INS
ORDER BY DATETIME 
LIMIT 1

#
SELECT NAME 
FROM ANIMAL_INS 
WHERE DATETIME = (SELECT MIN(DATETIME) from ANIMAL_INS);